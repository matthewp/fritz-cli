const { bundle } = require('./bundle.js');

async function buildServer() {
  /* 
  input,
  format,
  exportsType,
  external,
  chunks: chunkSep,
  name,
  out
  */ 
  await bundle({

  });
}

module.exports = buildServer;